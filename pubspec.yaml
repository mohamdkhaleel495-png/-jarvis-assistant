name: jarvis_app
description: "JARVIS Personal Voice AI"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  speech_to_text: ^6.6.1
  flutter_tts: ^4.0.2
  http: ^1.2.0

flutter:
  uses-material-design: true
